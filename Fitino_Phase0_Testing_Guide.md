# راهنمای تست فاز ۰ — چطور مطمئن بشم درست پیاده‌سازی شده؟
> این فایل رو مرحله‌به‌مرحله دنبال کن. هر تست یک ✅ یا ❌ واضح داره تا دقیقاً بدونی کجا مشکل داری.

---

## قبل از شروع: چی لازم داری؟

1. بک‌اند پروژه باید روی سیستمت **در حال اجرا** باشه (`go run` یا هرجوری که معمولاً اجراش می‌کنی)
2. یک ابزار برای فرستادن درخواست HTTP — یکی از این دو رو انتخاب کن:
   - **Postman** (ساده‌تر برای مبتدی، رابط گرافیکی داره) — دانلود از postman.com
   - **curl** (از طریق ترمینال/CMD، نیاز به نصب چیزی نداره)
3. یک **JWT token معتبر** برای لاگین (چون endpointها پشت `JWTAuthMiddleware` هستن)

### چطور JWT token بگیرم؟
با یوزر تستی که توی `PROJECT_INVENTORY.md` نوشته شده وارد شو:
```bash
curl -X POST http://localhost:8088/auth/login/password \
  -H "Content-Type: application/json" \
  -d '{"phone": "09150000000", "password": "12345678"}'
```
جوابی که می‌گیری یه چیزی شبیه اینه:
```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "refresh_token": "..."
}
```
مقدار `access_token` رو کپی کن — تا آخر این راهنما بهش نیاز داری. از این به بعد هرجا نوشتم `<TOKEN>` باید همین رو جاش بذاری.

---

## تست ۱ — آیا سرور اصلاً بالاست؟

```bash
curl http://localhost:8088/swagger/index.html
```
✅ **درست:** یک صفحه HTML برمی‌گرده (یا کد ۲۰۰)
❌ **مشکل:** اگه خطای `connection refused` گرفتی، یعنی سرور بک‌اند اصلاً اجرا نشده — اول اونو راه بنداز.

---

## تست ۲ — آیا `migration` جدول لاگ AI درست اجرا شده؟ (مربوط به BE-0.4)

باید ببینی جدول `ai_request_logs` واقعاً توی دیتابیس ساخته شده.

### روش ساده: مستقیم توی MySQL چک کن
```bash
mysql -u <DB_USER> -p<DB_PASSWORD> <DB_NAME> -e "SHOW TABLES LIKE 'ai_request_logs';"
```
✅ **درست:** اسم جدول `ai_request_logs` رو نشون می‌ده
❌ **مشکل:**
- اگه چیزی نشون نداد → یعنی `AutoMigrate` روی این مدل جدید اجرا نشده. برو مطمئن شو توی فایل بوت‌استرپ پروژه (معمولاً `internal/bootstrap/` یا جایی که مدل‌های دیگه Migrate می‌شن)، مدل `AIRequestLog` رو هم اضافه کردی.

می‌تونی ستون‌هاش رو هم چک کنی:
```bash
mysql -u <DB_USER> -p<DB_PASSWORD> <DB_NAME> -e "DESCRIBE ai_request_logs;"
```
باید ستون‌های `user_id`, `request_type`, `input_text`, `output_json`, `success`, `error_msg`, `created_at` رو ببینی.

---

## تست ۳ — تست اصلی: آیا AI واقعاً JSON منظم برمی‌گردونه؟ (مربوط به BE-0.1 و BE-0.2)

این مهم‌ترین تسته. فرض می‌کنیم endpoint فاز ۰ رو موقتاً روی یک مسیر تستی مثل `POST /me/nutrition/generate` گذاشتی.

### با curl:
```bash
curl -X POST http://localhost:8088/me/nutrition/generate \
  -H "Authorization: Bearer <TOKEN>" \
  -H "Content-Type: application/json" \
  -v
```

### با Postman:
1. یک request جدید بساز → Method: `POST`
2. آدرس: `http://localhost:8088/me/nutrition/generate`
3. برو تب **Authorization** → نوع رو بذار `Bearer Token` → مقدار `<TOKEN>` رو بذار
4. دکمه **Send** رو بزن

### چی باید ببینی؟

✅ **درست** — یک جواب شبیه این:
```json
{
  "goal_type": "cut",
  "total_calories": 2100,
  "protein_g": 160,
  "carbs_g": 200,
  "fat_g": 60,
  "meals": [
    {
      "name": "صبحانه",
      "items": [
        {
          "food_name": "تخم‌مرغ آب‌پز",
          "amount_g": 100,
          "calories": 155,
          "protein_g": 13,
          "carbs_g": 1.1,
          "fat_g": 11
        }
      ]
    }
  ]
}
```

### چک‌لیست دقیق روی همین جواب (خیلی مهم — فقط "یه جوابی اومد" کافی نیست):

| سوال | چطور چک کنم |
|------|--------------|
| آیا `meals` حداقل یک آیتم داره؟ | با چشم نگاه کن، نباید `[]` خالی باشه |
| آیا `total_calories` یک عدد منطقیه؟ (نه ۰، نه منفی، نه ۵۰۰۰۰۰) | باید بین ۸۰۰ تا ۶۰۰۰ باشه |
| آیا هر آیتم غذایی همه فیلدهاش پره؟ (نه `null`، نه رشته خالی) | هر `food_name`, `calories` و... باید مقدار داشته باشن |
| آیا خروجی *دقیقاً* JSON هست، نه یک متن که JSON توش قایم شده؟ | نباید چیزی شبیه `"اینم برنامه شما: { ... }"` ببینی؛ باید *فقط* JSON خام باشه |

❌ **مشکل رایج ۱:** جواب یک متن فارسی معمولیه، نه JSON  
→ یعنی `ResponseFormat` با `"type": "json_schema"` و `"strict": true` درست تنظیم نشده. برگرد به فایل `generator.go` چک کن.

❌ **مشکل رایج ۲:** خطای ۵۰۰ با پیام `"خطا در تبدیل جواب AI"`  
→ یعنی AI یک چیزی برگردونده که با `json.Unmarshal` جور درنمیاد. خروجی خام رو با `fmt.Println` توی لاگ سرور چاپ کن و ببین واقعاً چی برگشته.

❌ **مشکل رایج ۳:** خطای ۴۲۲ (`"برنامه تولیدشده معتبر نیست"`)  
→ خب! این در واقع نشونه خوبیه — یعنی لایه اعتبارسنجی (BE-0.3) داره درست کار می‌کنه و جلوی داده بد رو گرفته. فقط باید ببینی چرا AI داده بد تولید کرده (شاید پرامپت سیستم باید واضح‌تر بشه).

---

## تست ۴ — آیا اعتبارسنجی واقعاً کار می‌کنه؟ (مربوط به BE-0.3)

می‌خوایم عمداً یک داده خراب بسازیم و ببینیم سیستم جلوش رو می‌گیره یا نه. این تست رو مستقیم روی کد انجام بده (نه از طریق API)، چون AI معمولاً خودش داده خراب تولید نمی‌کنه.

یک فایل تست موقت بساز (مثلاً `backend/internal/service/ai/validate_test.go`):

```go
package ai

import "testing"

func TestValidateNutritionPlan_RejectsBadCalories(t *testing.T) {
	badPlan := &NutritionPlanSchema{
		TotalCalories: 50, // خیلی کمه، باید رد بشه
		Meals: []MealSchema{
			{Name: "صبحانه", Items: []FoodItem{{FoodName: "نان"}}},
		},
	}
	err := ValidateNutritionPlan(badPlan)
	if err == nil {
		t.Error("انتظار داشتیم خطا بگیریم ولی نگرفتیم — یعنی validation کار نمی‌کنه")
	}
}

func TestValidateNutritionPlan_RejectsEmptyMeals(t *testing.T) {
	badPlan := &NutritionPlanSchema{
		TotalCalories: 2000,
		Meals:         []MealSchema{}, // خالی، باید رد بشه
	}
	err := ValidateNutritionPlan(badPlan)
	if err == nil {
		t.Error("انتظار داشتیم خطا بگیریم ولی نگرفتیم")
	}
}
```

اجراش کن:
```bash
cd backend
go test ./internal/service/ai/... -v
```

✅ **درست:** هر دو تست `PASS` می‌شن (یعنی خطا رو درست تشخیص دادن)
❌ **مشکل:** اگه `FAIL` گرفتی، یعنی تابع `ValidateNutritionPlan` شرط‌هاش کامل نیست — برگرد کدشو چک کن.

---

## تست ۵ — آیا هر تماس با AI لاگ می‌شه؟ (مربوط به BE-0.4)

1. یک بار دیگه تست ۳ رو تکرار کن (یک درخواست موفق به `/me/nutrition/generate` بزن)
2. بعدش این رو اجرا کن:
```bash
mysql -u <DB_USER> -p<DB_PASSWORD> <DB_NAME> -e "SELECT id, user_id, request_type, success, created_at FROM ai_request_logs ORDER BY id DESC LIMIT 1;"
```

✅ **درست:** یک ردیف جدید می‌بینی با `success = 1` و `created_at` همین چند لحظه پیش
❌ **مشکل:** اگه چیزی نیومد یعنی توی کنترلر، بعد از گرفتن جواب AI، فراموش کردی رکورد `AIRequestLog` رو `Create` کنی. برگرد و این خط رو اضافه کن.

**تست تکمیلی (لاگ خطا):** یک بار عمداً `OPENAI_API_KEY` رو توی `.env` اشتباه بذار (یک حرف عوض کن)، سرور رو ری‌استارت کن، دوباره تست ۳ رو بزن (باید خطا بگیری)، و چک کن که یک ردیف با `success = 0` و `error_msg` پر، توی جدول اضافه شده باشه. بعد `.env` رو درست کن و دوباره ری‌استارت کن.

---

## تست ۶ — آیا Persona (چند شخصیت AI) کار می‌کنه؟ (مربوط به BE-0.5)

باید ببینی وقتی `persona` رو عوض می‌کنی، رفتار AI واقعاً تغییر می‌کنه.

### تست با curl — یک بار با persona تغذیه:
```bash
curl -X POST http://localhost:8088/me/ai/chat \
  -H "Authorization: Bearer <TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{"persona": "nutrition", "message": "بهترین حرکت برای سینه چیه؟"}'
```

✅ **درست:** AI باید یک چیزی شبیه بگه *"من فقط می‌تونم درباره تغذیه کمکت کنم"* یا بحث رو به سمت تغذیه ببره (چون تو System Prompt گفتیم فقط راجع به تغذیه صحبت کنه)

### حالا همون سوال رو با persona تمرین بپرس:
```bash
curl -X POST http://localhost:8088/me/ai/chat \
  -H "Authorization: Bearer <TOKEN>" \
  -H "Content-Type: application/json" \
  -d '{"persona": "workout", "message": "بهترین حرکت برای سینه چیه؟"}'
```

✅ **درست:** این بار باید یک جواب واقعی و مفصل درباره حرکات سینه بده (پرس سینه، شنا و...)

❌ **مشکل:** اگه جواب هر دو حالت **کاملاً یکسانه**، یعنی `persona` واقعاً به `SystemPrompt()` وصل نشده — سیستم داره اون پارامتر رو نادیده می‌گیره. کنترلر چت رو چک کن ببین `persona` از بدنه درخواست خونده می‌شه یا نه.

---

## تست ۷ — تست نهایی سرتاسری (End-to-End)

این آخرین تسته که همه‌چیز رو با هم چک می‌کنه — انگار داری از دید یک کاربر واقعی امتحان می‌کنی.

**سناریو:** یک کاربر تازه لاگین‌کرده، اطلاعات پروفایلش کامله (قد/وزن/هدف)، و می‌خواد برنامه غذایی بگیره.

1. با یک کاربر واقعی (نه فقط ادمین تستی) که پروفایلش پر شده لاگین کن
2. یک بار endpoint تولید برنامه غذایی رو صدا بزن
3. با چشم بررسی کن:
   - [ ] کالری کل با توجه به قد/وزن/هدف اون کاربر منطقیه؟ (مثلاً یک آدم برای کات نباید ۴۰۰۰ کالری بگیره)
   - [ ] تعداد وعده‌ها منطقیه (۳ تا ۶ وعده)؟
   - [ ] جمع کالری تک‌تک آیتم‌ها تقریباً با `total_calories` جور درمیاد؟
   - [ ] یک رکورد جدید توی `ai_request_logs` ثبت شده؟
4. همین کاربر رو یک بار دیگه امتحان کن با پروفایل هدف متفاوت (مثلاً از "کات" به "بالک" عوضش کن) و ببین AI واقعاً برنامه رو عوض می‌کنه یا هر بار یک چیز مشابه می‌ده

✅ اگه همه این‌ها درست بود → **فاز ۰ کامل و آماده‌ست**، می‌تونی بری سراغ فاز ۱.

---

## جدول خلاصه — همه تست‌ها یک‌جا

| # | تست چی رو چک می‌کنه | ابزار | نتیجه موفق |
|---|----------------------|-------|-------------|
| ۱ | سرور بک‌اند بالاست | curl | کد ۲۰۰ از swagger |
| ۲ | جدول لاگ AI ساخته شده | mysql | جدول `ai_request_logs` هست |
| ۳ | AI خروجی JSON منظم می‌ده | curl/Postman | JSON کامل و معتبر برمی‌گرده |
| ۴ | اعتبارسنجی داده خراب رو رد می‌کنه | `go test` | تست‌ها PASS می‌شن |
| ۵ | هر تماس AI لاگ می‌شه | mysql | ردیف جدید بعد هر درخواست |
| ۶ | Persona رفتار AI رو عوض می‌کنه | curl | جواب‌های متفاوت برای persona متفاوت |
| ۷ | سناریوی واقعی کاربر درست کار می‌کنه | دستی | برنامه منطقی و متناسب با پروفایل |

---

## اگه همه‌چیز خراب بود و نمی‌دونی از کجا شروع کنی

از تست ۱ شروع کن و به ترتیب برو جلو — هر تستی که ❌ شد، همون‌جا وایسا و مشکلشو حل کن، بعد برو تست بعدی. هیچ‌وقت تست ۳ به بعد رو امتحان نکن اگه تست ۱ و ۲ رد شدن، چون بقیه به این دو تا وابسته‌ان.

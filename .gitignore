# Local task notes (not for version control)
task.txt

# crul large generated media (do not upload)
crul/output/food/images/
crul/output/exercise/images/
crul/output/exercise/videos/

uploads/
node_modules
.idea

.cursor/*
.specify/*

# Local Go build cache (never commit)
.gocache/

# ML / ASR large weights (~450MB onnx — download on pull via .githooks)
ml/models/**/*.onnx
ml/models/.download.log
ml/models/**/.cache/
ml/**/__pycache__/
ml/**/*.egg-info/
ml/.venv/

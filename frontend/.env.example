# Copy to .env.local for local development (do not commit secrets).
# Production builds omit .env.local and fall back to https://api.fitinoo.ir in code.

NEXT_PUBLIC_API_BASE_URL=http://localhost:8088

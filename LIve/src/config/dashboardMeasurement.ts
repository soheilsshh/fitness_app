/**
 * Campaign measurement start (Jalali YYYY/MM/DD).
 * Set when day 21 (or campaign start) begins — until then leave null.
 * Example: '1404/06/21'
 */
export const DASHBOARD_MEASUREMENT_START_DATE: string | null = null;

# ✅ تضمین شروع روزانه استریم از ابتدا

## 🎯 سوال

**آیا مطمئن هستیم که:**
1. هر روز راس ساعت 8 عصر (20:00) استریم از 0 شروع می‌شود؟
2. وقتی تموم شد، فردا دوباره راس ساعت 8 عصر از 0 شروع می‌شود؟

## ✅ پاسخ: بله، کاملاً مطمئن هستیم!

---

## 🔍 بررسی فنی

### ۱. Scheduler هر روز در 20:00 اجرا می‌شود

**کد:**
```go
// backend/scheduler/scheduler.go:179
s.Every(1).Day().At(streamTimeStr).Do(func() {
    log.Println("📹 Running job: Start streaming for today's webinar")
    startStreamingForToday(db, cfg)
})
```

**نتیجه:** ✅ هر روز در ساعت 20:00 (از config) scheduler اجرا می‌شود

---

### ۲. Wait واقعی تا زمان شروع

**کد:**
```go
// backend/streaming/server.go:301-304
if waitDuration > 0 {
    log.Printf("⏳ Waiting %v until webinar starts at %s...", waitDuration, ...)
    time.Sleep(waitDuration)  // واقعاً صبر می‌کند!
    log.Printf("✅ Wait completed. Starting stream now at %s", ...)
}
```

**نتیجه:** ✅ استریم دقیقاً در زمان شروع وبینار شروع می‌شود (نه قبل، نه بعد)

---

### ۳. فایل همیشه از ابتدا باز می‌شود

**کد:**
```go
// backend/streaming/server.go:325
// CRITICAL: Always open file fresh to ensure we start from beginning (time 0)
file, err := avutil.Open(filePath)
```

**نتیجه:** ✅ فایل همیشه به صورت fresh باز می‌شود (نه از آخرین موقعیت)

---

### ۴. firstPacketTime از اولین packet تنظیم می‌شود

**کد:**
```go
// backend/streaming/server.go:345, 364-372
firstPacketTime := time.Duration(-1)

if firstPacketTime == -1 {
    firstPacketTime = pkt.Time
    log.Printf("🎬 First packet time: %v (starting from beginning)", firstPacketTime)
}
```

**نتیجه:** ✅ timing همیشه از اولین packet محاسبه می‌شود (time 0)

---

### ۵. Flag بعد از پایان reset می‌شود

**کد:**
```go
// backend/streaming/server.go:389-392
log.Println("Stream has concluded.")

// Mark stream as finished
streamMutex.Lock()
streamRunning = false
streamMutex.Unlock()
```

**نتیجه:** ✅ بعد از پایان استریم، flag reset می‌شود و استریم بعدی می‌تواند شروع شود

---

### ۶. بررسی و reset خودکار flag (اگر استریم قبلی تمام شده باشد)

**کد:**
```go
// backend/streaming/server.go:275-281
// If previous stream's end time has passed, reset the flag (stream finished)
if streamRunning && now.After(currentStreamEndTime) {
    log.Printf("🔄 Previous stream ended. Resetting flag for new stream.")
    streamRunning = false
}
```

**نتیجه:** ✅ اگر استریم قبلی تمام شده باشد (endTime گذشته)، flag خودکار reset می‌شود

---

## 📊 چرخه روزانه

### روز ۱ (دوشنبه):
```
20:00 → Scheduler اجرا می‌شود
      → Wait تا 20:00 (0 ثانیه)
      → استریم از ابتدا (time 0) شروع می‌شود
22:00 → استریم تمام می‌شود
      → streamRunning = false
```

### روز ۲ (سه‌شنبه):
```
20:00 → Scheduler اجرا می‌شود
      → بررسی: streamRunning = false ✅
      → Wait تا 20:00 (0 ثانیه)
      → استریم از ابتدا (time 0) شروع می‌شود
22:00 → استریم تمام می‌شود
      → streamRunning = false
```

### روز ۳ (چهارشنبه):
```
20:00 → Scheduler اجرا می‌شود
      → بررسی: streamRunning = false ✅
      → Wait تا 20:00 (0 ثانیه)
      → استریم از ابتدا (time 0) شروع می‌شود
...
```

---

## 🛡️ محافظت‌های اضافه شده

### ۱. جلوگیری از شروع چند استریم همزمان

```go
if streamRunning {
    log.Printf("⚠️  Stream is already running. Skipping new stream request.")
    return
}
```

**نتیجه:** ✅ فقط یک استریم در هر زمان می‌تواند اجرا شود

---

### ۲. Reset خودکار flag (اگر استریم قبلی تمام شده باشد)

```go
if streamRunning && now.After(currentStreamEndTime) {
    streamRunning = false  // Reset برای استریم جدید
}
```

**نتیجه:** ✅ حتی اگر flag reset نشده باشد، خودکار reset می‌شود

---

### ۳. بررسی زمان شروع در scheduler

```go
timeUntilStart := webinar.StartTime.Sub(now)
if timeUntilStart > 5*time.Minute {
    log.Printf("⏳ Too early to start stream, skipping")
    return
}
```

**نتیجه:** ✅ فقط در زمان مناسب (20:00 ± 5 دقیقه) استریم شروع می‌شود

---

## 🧪 سناریوهای تست

### سناریو ۱: استریم دیروز تمام شد ✅

**وضعیت:**
- دیروز: استریم 20:00-22:00 اجرا شد
- Flag: `streamRunning = false` ✅

**فردا 20:00:**
- Scheduler اجرا می‌شود
- بررسی: `streamRunning = false` ✅
- استریم جدید شروع می‌شود ✅
- از ابتدا (time 0) ✅

---

### سناریو ۲: استریم دیروز هنوز در حال اجرا است (غیرعادی) ✅

**وضعیت:**
- دیروز: استریم 20:00 شروع شد اما هنوز در حال اجرا است
- Flag: `streamRunning = true`
- EndTime: دیروز 22:00 (گذشته)

**فردا 20:00:**
- Scheduler اجرا می‌شود
- بررسی: `streamRunning = true` اما `now.After(currentStreamEndTime)` ✅
- Flag reset می‌شود: `streamRunning = false` ✅
- استریم جدید شروع می‌شود ✅
- از ابتدا (time 0) ✅

---

### سناریو ۳: استریم امروز در حال اجرا است (تداخل) ✅

**وضعیت:**
- امروز: استریم 20:00 شروع شد
- Flag: `streamRunning = true`
- EndTime: امروز 22:00 (آینده)

**امروز 20:05 (scheduler دوباره اجرا می‌شود - غیرعادی):**
- Scheduler اجرا می‌شود
- بررسی: `streamRunning = true` و `now.Before(currentStreamEndTime)` ✅
- استریم جدید رد می‌شود ✅
- استریم قبلی ادامه پیدا می‌کند ✅

---

## 📝 خلاصه تضمین‌ها

✅ **تضمین ۱:** هر روز راس ساعت 20:00 scheduler اجرا می‌شود

✅ **تضمین ۲:** استریم دقیقاً در 20:00 شروع می‌شود (با wait واقعی)

✅ **تضمین ۳:** ویدیو همیشه از ابتدا (time 0) شروع می‌شود

✅ **تضمین ۴:** بعد از پایان، flag reset می‌شود

✅ **تضمین ۵:** اگر flag reset نشده باشد، خودکار reset می‌شود

✅ **تضمین ۶:** فقط یک استریم در هر زمان می‌تواند اجرا شود

---

## 🎯 نتیجه نهایی

**بله، کاملاً مطمئن هستیم که:**

1. ✅ هر روز راس ساعت 8 عصر (20:00) استریم از 0 شروع می‌شود
2. ✅ وقتی تموم شد، فردا دوباره راس ساعت 8 عصر از 0 شروع می‌شود
3. ✅ همه محافظت‌ها و بررسی‌ها در جای خود هستند
4. ✅ سیستم به صورت کامل حلقه‌ای و قابل اعتماد کار می‌کند

---

**آخرین به‌روزرسانی:** ۲۰۲۵


# موجودی فعالیت‌ها و پروفایل برای سیستم مدال‌دهی فیتینو

> منبع: مدل‌های بک‌اند (`user`, `user_photo`, `check_in`, …)، UI پروفایل/پایش، ناوبری پنل کاربر، و قوانین فعلی `achievement_service`  
> هدف: طراحی سیستم نمره‌دهی و مدال بر اساس سختی واقعی هر عمل  
> تاریخ: ۱۲ اوت ۲۰۲۶

---

## مقیاس سختی پیشنهادی (۱–۵)

| سطح | نام | معنی طراحی |
|-----|-----|------------|
| ۱ | خیلی آسان | یک‌بار / چند ثانیه / بدون عادت‌سازی |
| ۲ | آسان | چند فیلد یا یک آپلود ساده |
| ۳ | متوسط | عمل روزانه/جلسه‌ای با کمی اصطکاک |
| ۴ | سخت | تکرار منظم چندروزه یا ثبت چند‌جزئی |
| ۵ | خیلی سخت | تداوم بلندمدت، PR واقعی، یا تکمیل کامل پروفایل+پایش |

**امتیاز پایه پیشنهادی:** `امتیاز ≈ سختی × ۱۰` (قابل تنظیم؛ قوانین فعلی seed را پایین ببین).

**انواع مدال:** یک‌بار (one-time) · تکرارپذیر (repeatable) · استریک (streak) · مایلستون (milestone)

---

## ۱) تکمیل پروفایل دانشجو

در بک‌اند تکمیل پروفایل **۴ بخش** دارد (`StudentProfileProgress`) و درصد = تعداد بخش‌های کامل × ۲۵٪.

### ۱.۱ گیت دسترسی پنل (حداقل اجباری)

برای دانشجو، پنل «کامل» محسوب می‌شود فقط وقتی:

| فیلد | توضیح | سختی | امتیاز پیشنهادی | مدال پیشنهادی |
|------|--------|------|-----------------|---------------|
| نام واقعی | نه خالی، نه «کاربر جدید» | ۱ | ۵ | — (پیش‌نیاز، بدون مدال جدا) |
| هدف اصلی / اهداف | `primary_goal` یا حداقل یک آیتم در `goals` | ۱ | ۵ | — |

> بدون این دو مورد، `IsStudentProfileComplete = false`.

### ۱.۲ بخش Essentials / اطلاعات شخصی (UI: تب «اطلاعات شخصی»)

| فیلد | مقادیر / نکته | لازم برای % پروفایل؟ | سختی | امتیاز | مدال |
|------|----------------|----------------------|------|--------|------|
| نام | firstName | بخشی از نام کامل | ۱ | ۲ | — |
| نام خانوادگی | lastName | بخشی از نام کامل | ۱ | ۲ | — |
| جنسیت | `male` / `female` | در progress بک‌اند نه؛ در UI بخش personal هست | ۱ | ۳ | — |
| تاریخ تولد | شمسی در UI → میلادی در API | اختیاری برای % بک‌اند | ۱ | ۵ | — |
| کد ملی | ۱۰ رقم، اعتبارسنجی ایران | اختیاری | ۲ | ۸ | — |
| ایمیل / تلفن | از ثبت‌نام؛ تلفن معمولاً قفل | از قبل پر | — | — | — |
| آواتار | `avatar_url` | اختیاری | ۱ | ۵ | «چهرهٔ پروفایل» (یک‌بار) |
| تغییر رمز عبور | اکشن حساب | اختیاری امنیتی | ۱ | — | — |

### ۱.۳ بخش Body / اهداف و اندازه‌ها (برای `body = true`)

همهٔ موارد زیر باید پر باشند تا بخش بدن کامل شود:

| فیلد | مقادیر | سختی | امتیاز | مدال |
|------|--------|------|--------|------|
| قد (`height_cm`) | > 0 | ۱ | ۵ | — |
| وزن فعلی (`weight_kg`) | > 0 | ۱ | ۵ | — |
| وزن هدف (`target_weight_kg`) | > 0 | ۱ | ۵ | — |
| وضعیت بدنی (`body_condition`) | `slim` · `average` · `overweight` · `athletic` · `muscular` | ۱ | ۵ | — |
| هدف(ها) (`goals` / `primary_goal`) | `weight_loss` · `muscle_gain` · `fitness` · `endurance` · `flexibility` · `rehabilitation` | ۱ | ۵ | — |
| درصد چربی بدن (`body_fat_percent`) | اختیاری (در progress بک‌اند لازم نیست) | ۲ | ۸ | — |

**مدال بخش:** «پایه بدن تکمیل شد» — یک‌بار — سختی ۲ — **۲۰ امتیاز** (وقتی `body=true`).

### ۱.۴ بخش Medical / سوابق پزشکی (برای `medical = true`)

| فیلد | نکته UI | سختی | امتیاز | مدال |
|------|---------|------|--------|------|
| سابقه پزشکی (`medical_history`) | می‌تواند «ندارم» باشد ولی باید مقدار داشته باشد | ۲ | ۸ | — |
| آسیب‌ها (`injuries`) | همین‌طور | ۲ | ۸ | — |
| محدودیت‌های فیزیکی (`physical_limitations`) | همین‌طور | ۲ | ۸ | — |

**مدال بخش:** «سلامت شفاف» — یک‌بار — سختی ۲ — **۲۵ امتیاز** (وقتی `medical=true`).

### ۱.۵ بخش Photos / عکس‌های اولیه بدن (برای `photos = true`)

عکس‌های **ثبت‌نام اولیه** (`CheckInDate == null`) با ۴ زاویه اجباری:

| نوع (`type`) | برچسب UI | سختی تک‌عکس | امتیاز تک‌عکس | نکته |
|--------------|----------|-------------|---------------|------|
| `front` | جلو | ۲ | ۸ | لباس چسبان، نور یکنواخت |
| `right` | راست | ۲ | ۸ | دوربین هم‌ارتفاع کمر |
| `back` | عقب | ۲ | ۸ | پس‌زمینه ساده |
| `left` | چپ | ۲ | ۸ | — |

**مدال بخش:** «آلبوم اولیه کامل» — یک‌بار — سختی ۳ — **۴۰ امتیاز** (هر ۴ زاویه).

**مدال نهایی پروفایل:** «پروفایل ۱۰۰٪» — یک‌بار — سختی ۳ — **۷۵ امتیاز** (هر ۴ بخش `essentials+body+medical+photos`).

---

## ۲) عکس‌هایی که کاربر باید بگذارد

### ۲.۱ عکس پروفایل (اختیاری)

| مورد | مسیر تقریبی | سختی | امتیاز | مدال |
|------|-------------|------|--------|------|
| آواتار حساب | پروفایل | ۱ | ۵ | «چهرهٔ پروفایل» |

### ۲.۲ عکس‌های اولیه بدن (۴ زاویه — ثبت‌نام)

| زاویه | کد | اجباری برای % عکس پروفایل |
|-------|----|---------------------------|
| جلو | `front` | ✅ |
| راست | `right` | ✅ |
| عقب | `back` | ✅ |
| چپ | `left` | ✅ |

### ۲.۳ عکس‌های پایش پیشرفت (هر ~۱۴ روز — `DefaultCheckinFrequencyDays = 14`)

در پایش (`/user/tracking`) سه زاویه لازم است (`TrackingPhotoTypes`):

| زاویه | کد | برچسب UI پایش |
|-------|----|----------------|
| جلو | `front` | جلو |
| عقب | `back` | پشت |
| بغل | `side` | بغل |

| اکشن | سختی | امتیاز | مدال پیشنهادی |
|------|------|--------|----------------|
| آپلود ۱ زاویه پایش در دوره | ۲ | ۸ | تکرارپذیر کوچک |
| تکمیل ۳ زاویه در یک دورهٔ ۱۴روزه | ۴ | ۴۰ | «چک‌این تصویری کامل» (تکرارپذیر هر دوره) |
| ۴ دوره پیاپی چک‌این عکس کامل | ۵ | ۱۲۰ | «پایداری بصری ۲ ماهه» (یک‌بار یا هر فصل) |

> عکس‌های پایش با `CheckInDate != null` از عکس‌های اولیه جدا می‌شوند و می‌توانند تحلیل AI + بازبینی مربی داشته باشند.

### ۲.۴ رسانه اجتماعی (اختیاری)

| اکشن | سختی | امتیاز | مدال |
|------|------|--------|------|
| پست با عکس/ویدیو در فید | ۲ | ۱۰ | «اولین اشتراک» (یک‌بار) + امتیاز تکرارپذیر کم |

---

## ۳) کارهایی که کاربر در اپ می‌تواند انجام دهد

بر اساس ناوبری پنل دانشجو + مدل‌های بک‌اند.

### ۳.۱ خانه / داشبورد

| فعالیت | توضیح | سختی | امتیاز | مدال |
|--------|--------|------|--------|------|
| مشاهده داشبورد | جلسات هفته/ماه، استریک هفتگی، adherence، PRها | ۱ | ۰–۱ | — (مشاهده جایزه ندارد) |
| رسیدن به هدف هفتگی تمرین | تکمیل روزهای برنامه در هفته | ۴ | ۳۰ | «هفتهٔ منظم» (تکرارپذیر هفتگی) |
| استریک چند هفته تمرین | `streakWeeks` متوالی | ۵ | ۵۰ / ۱۰۰ / ۲۰۰ | برنز ۷روز · نقره ۴هفته · طلا ۱۲هفته |

### ۳.۲ تمرین

| فعالیت | داده مرتبط | سختی | امتیاز | مدال |
|--------|------------|------|--------|------|
| دیدن برنامه فعال | `workout_program` / my-programs | ۱ | — | — |
| ثبت جلسه تمرین | `WorkoutSession` (روز، مدت، تعداد حرکت) | ۳ | ۱۵ | «اولین جلسه» (یک‌بار) + تکرارپذیر روزانه |
| ثبت ست‌ها | `WorkoutSetLog` (وزنه/تکرار) | ۳ | +۲ به ازای ست معنادار | — |
| گزارش حس بعد تمرین | `EffortRPE` · `FeelingAfter` · `SatisfactionRating` | ۲ | ۵ اضافه | «بازخورد صادقانه» (استریک ۷روز گزارش) |
| ثبت PR جدید | بهترین وزنه/تکرار نسبت به قبل | ۴ | **۱۰ (فعلی)** | «رکورد جدید» — **موجود و تکرارپذیر** |
| استریک ۷ روز تمرین | حداقل ۱ جلسه در ۷ روز متوالی | ۵ | **۵۰ (فعلی)** | «تداوم تمرین یک‌هفته‌ای» — **موجود، یک‌بار** |
| پیشنهاد توسعه | استریک ۳۰ / ۹۰ روز تمرین | ۵ | ۱۵۰ / ۴۰۰ | مدال‌های بلندمدت |

### ۳.۳ تغذیه

| فعالیت | داده مرتبط | سختی | امتیاز | مدال |
|--------|------------|------|--------|------|
| ثبت یک آیتم غذا | `DailyFoodLog` (+ کالری/ماکرو/وعده) | ۲ | ۳ | تکرارپذیر روزانه با سقف |
| تکمیل کالری/ماکرو روز | رسیدن نزدیک به تارگت | ۳ | ۱۵ | «روز تغذیه‌ای کامل» |
| استریک ۳۰ روز ثبت غذا | حداقل ۱ لاگ در ۳۰ روز متوالی | ۵ | **۱۰۰ (فعلی)** | «پیوستگی ۳۰روزه ثبت غذا» — **موجود، یک‌بار** |
| پیشنهاد توسعه | استریک ۷ / ۹۰ روز غذا | ۴–۵ | ۴۰ / ۲۵۰ | مدال‌های میانی/بلند |
| ساخت/ویرایش برنامه تغذیه با AI | `/user/ai-programs` | ۳ | ۲۰ | «اولین برنامه AI تغذیه» (یک‌بار) |

### ۳.۴ دستیار هوشمند (AI)

| فعالیت | مسیر | سختی | امتیاز | مدال |
|--------|------|------|--------|------|
| ساخت برنامه تمرین با AI | ai-programs/new | ۳ | ۲۰ | «معمار تمرین AI» |
| ساخت برنامه تغذیه با AI | ai-programs/new | ۳ | ۲۰ | «معمار تغذیه AI» |
| ویرایش برنامه با AI | ai-programs/edit | ۲ | ۱۰ | تکرارپذیر با سقف هفتگی |
| چت با مربی AI | `/user/ai` (در صورت فعال) | ۲ | ۲ / پیام معنادار | سقف روزانه |

### ۳.۵ پایش پیشرفت (Tracking / Check-in)

| فعالیت | فیلدها | سختی | امتیاز | مدال |
|--------|--------|------|--------|------|
| ثبت وزن دوره‌ای | `CheckIn.Weight` یا `/me/tracking/weight` | ۲ | ۱۰ | «ترازوی منظم» |
| ثبت اندازه‌ها | کمر / سینه / باسن (`Waist`/`Chest`/`Hip`) | ۳ | ۱۵ | «اندازه‌گیری کامل» |
| آپلود ۳ عکس پایش | front/back/side | ۴ | ۴۰ | بالا در بخش عکس |
| تکمیل چک‌این ۱۴روزه کامل | وزن + ۳ عکس (+ ideally اندازه‌ها) | ۴ | ۶۰ | «چک‌این طلایی» (هر دوره) |
| ۴ چک‌این کامل پیاپی | ≈ ۲ ماه | ۵ | ۱۵۰ | «پایداری پایش» |

### ۳.۶ اجتماع و رویداد

| فعالیت | مدل | سختی | امتیاز | مدال |
|--------|-----|------|--------|------|
| پست متنی | `CommunityPost` | ۲ | ۵ | «اولین پست» |
| پست با رسانه | image/video | ۲ | ۱۰ | — |
| دسته‌بندی پست | workout/nutrition/progress/record/question/tip | ۱ | +۲ | — |
| کامنت / لایک | `PostComment` / `PostLike` | ۱ | ۱ | سقف روزانه |
| RSVP رویداد | `EventParticipation` | ۲ | ۱۵ | «حضور در رویداد» |
| شرکت در مسابقه/جایزه | event با prize | ۳–۴ | ۳۰–۵۰ | بر اساس نوع رویداد |

### ۳.۷ مربی انسانی و پشتیبانی

| فعالیت | مدل | سختی | امتیاز | مدال |
|--------|-----|------|--------|------|
| شرکت در جلسه آنلاین/حضوری | `CoachSession` completed | ۳ | ۲۵ | «جلسه با مربی» (تکرارپذیر) |
| ارسال تیکت به مربی | `Ticket` | ۲ | ۵ | سقف هفتگی |
| دریافت/خواندن فیدبک مربی | `CoachReview` | ۱ | ۳ | — |

### ۳.۸ حساب، سفارش، تضمین، اعلان

| فعالیت | توضیح | سختی | امتیاز | مدال |
|--------|--------|------|--------|------|
| خرید پلن / سفارش موفق | `Order` paid | ۳ | ۳۰ | «شروع مسیر» (یک‌بار اولین خرید) |
| تمدید اشتراک | subscription renew | ۳ | ۲۰ | «ادامه مسیر» |
| فعال نگه‌داشتن نوتیف | `NotificationsEnabled` | ۱ | ۵ | — |
| درخواست تضمین با پایبندی بالا | `GuaranteeCase` + compliance بالا | ۵ | ۰–۵۰* | بهتر: مدال «پایبندی تضمینی» برای ٪ بالا، نه برای خود claim |
| عضویت ۵ ساله | از تاریخ ثبت‌نام | ۵ | **۵۰۰ (فعلی)** | «مدال ۵ سال سابقه» — **موجود** |

\* برای تضمین، مدال را به **رعایت شرط** بده نه به ثبت شکایت.

### ۳.۹ محتوا (در صورت استفاده)

| فعالیت | مسیر | سختی | امتیاز | مدال |
|--------|------|------|--------|------|
| تماشای محتوای آکادمی | `/user/academy` | ۱ | ۲ | سقف روزانه |
| مطالعه FAQ | `/user/faq` | ۱ | — | — |

---

## ۴) قوانین مدال فعلی در کد (baseline)

Seed در `achievement_service.go`:

| کد | عنوان | امتیاز | تکرار | تریگر |
|----|--------|--------|-------|--------|
| `pr_new` | رکورد جدید | ۱۰ | ✅ | PR در ثبت ست |
| `food_streak_30` | پیوستگی ۳۰روزه ثبت غذا | ۱۰۰ | یک‌بار | استریک غذا |
| `workout_streak_7` | تداوم تمرین یک‌هفته‌ای | ۵۰ | یک‌بار | استریک جلسه |
| `membership_5_years` | مدال ۵ سال سابقه | ۵۰۰ | یک‌بار | سن اکانت |

این سند قوانین **پیشنهادی گسترش** را روی همین موتور (`achievement_rule` + `user_achievement`) سوار می‌کند.

---

## ۵) ماتریس اولویت پیاده‌سازی مدال (پیشنهاد محصول)

### فاز A — قلاب‌های عادت روزانه (بیشترین اثر)
1. ثبت غذا روزانه / استریک ۷ و ۳۰  
2. ثبت جلسه تمرین / استریک ۷ و ۳۰  
3. PR جدید (موجود)

### فاز B — تکمیل هویت و کیفیت داده
4. پروفایل ۱۰۰٪ (+ زیربخش‌های body/medical/photos)  
5. آواتار + اولین پست جامعه  

### فاز C — پایش فیزیکی
6. چک‌این وزن ۱۴روزه  
7. ۳ عکس پایش کامل در دوره  
8. چک‌این طلایی (وزن+عکس+اندازه)

### فاز D — تعامل عمیق
9. جلسه با مربی  
10. RSVP رویداد  
11. پایبندی هفتگی به برنامه (adherence)  
12. مایلستون عضویت (۱سال / ۵سال)

---

## ۶) قواعد طراحی ضدتقلب (کوتاه)

- سقف امتیاز روزانه برای اکشن‌های ارزان (لایک، چت، تماشا).  
- استریک فقط با **روز تقویمی متمایز** (همان منطق فعلی `consecutiveDayStreak`).  
- عکس پایش: یک ست کامل در هر `frequencyDays`، نه آپلود اسپم.  
- PR: فقط اگر وزنه/تکرار نسبت به بهترین قبلی واقعاً بهتر باشد.  
- مدال‌های یک‌بار را در UI قفل/باز کن؛ تکرارپذیرها را با شمارنده نشان بده.

---

## ۷) خلاصه سریع برای طراح مدال

| دسته | تعداد اکشن اصلی | بازه سختی | بازه امتیاز پیشنهادی |
|------|------------------|-----------|----------------------|
| پروفایل متنی | ~۱۵ فیلد | ۱–۲ | ۲–۸ هر فیلد / ۲۰–۷۵ بخش |
| عکس | ۴ اولیه + ۳ پایش + آواتار | ۱–۴ | ۵–۴۰ |
| تغذیه | لاگ روزانه + استریک | ۲–۵ | ۳–۱۰۰+ |
| تمرین | جلسه + ست + PR + استریک | ۳–۵ | ۱۰–۵۰+ |
| پایش | وزن/سایز/عکس دوره‌ای | ۲–۵ | ۱۰–۱۵۰ |
| AI | ساخت/ویرایش برنامه | ۲–۳ | ۱۰–۲۰ |
| اجتماع/رویداد | پست/لایک/RSVP | ۱–۴ | ۱–۵۰ |
| مربی/خرید/عضویت | جلسه، سفارش، ۵سال | ۱–۵ | ۵–۵۰۰ |

---

*این فایل موجودی قابل‌اجرا برای طراحی قوانین `achievement_rule` است؛ امتیازهای «پیشنهادی» غیر از جدول بخش ۴ هنوز در کد seed نشده‌اند.*

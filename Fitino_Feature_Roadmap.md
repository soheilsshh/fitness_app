# نقشه راه فیچرهای جدید فیتینو (Fitino)
> بر اساس تحلیل `PROJECT_INVENTORY.md` (استک واقعی کد) + یادداشت‌های صوتی/دستی فیچرها (PDF)
> تاریخ تهیه: ۱۹ مرداد ۱۴۰۵ (۱۰ اوت ۲۰۲۶)

---

## ۰) خلاصه مدیریتی

یادداشت‌های تو حول **۷ حوزه اصلی** می‌چرخن که همه‌شون روی معماری فعلی فیتینو (Go/Gin/GORM + Next.js + Flutter، با نقش‌های admin/coach/student، و `ai_chat_controller` موجود با OpenAI) قابل پیاده‌سازی هستن. هیچ‌کدوم نیاز به تغییر استک ندارن؛ همه‌شون **گسترش** مدل‌ها و کنترلرهای موجودن، نه بازنویسی.

| # | حوزه در یادداشت | نگاشت به پروژه فعلی |
|---|------------------|----------------------|
| ۱ | مربی AI (دو نوع مربی) | گسترش `ai_chat_controller` موجود به چند «شخصیت/mode» |
| ۲ | تغذیه هوشمند | گسترش `nutrition_program` + `food` + `daily_food_log` |
| ۳ | تمرین هوشمند | گسترش `workout_program` + `workout_session` + `workout_set_log` |
| ۴ | پیشرفت و آمار | مدل جدید Achievement/Stats روی داده‌های موجود tracking |
| ۵ | انگیزه و استمرار | نوتیفیکیشن + `notification.go` موجود |
| ۶ | Gamification | مدل جدید امتیاز/مدال + گسترش `coach_achievement` برای دانشجو |
| ۷ | مربی انسانی + Deep Dive AI + تضمین‌ها | گسترش `ticket` + جاب‌های Cron روی AI |

---

## ۱) دسته‌بندی کامل فیچرهای PDF

### دسته A — مربی هوش مصنوعی (زیرساخت مشترک همه بخش‌ها)
- A1. چت‌بات تخصصی ورزش/تغذیه که به سوالات پاسخ می‌ده و بر اساس هدف/مشکل کاربر راهنمایی می‌کنه
- A2. قابلیت نوشتن غذای خاص برای فرد (خروجی ساختاریافته، نه فقط متن)

### دسته B — تغذیه هوشمند
- B1. **سازنده برنامه تغذیه هوشمند**: محاسبه متابولیسم پایه (BMR/TDEE) از سن، قد، وزن، جنسیت، سابقه تمرینی، بیماری؛ تعیین هدف تغذیه‌ای (کات/بالک/نگهداری)؛ نوشتن برنامه کالری و ماکرو
- B2. **تحلیل تغییرات ماهانه**: وزن، عکس، قدرت/عملکرد (بر اساس پیشرفت وزنه و تکرار) نسبت به ماه قبل
- B3. **بانک غذایی فیلترپذیر**: فیلتر بر اساس کالری، نوع غذا (فست‌فود رژیمی، سنتی رژیمی و…)، مواد موجود کاربر؛ هر آیتم شامل دستور تهیه + ویدیو + توضیحات
- B4. **دستور غذای بداهه**: بر اساس مواد غذایی که کاربر *الان* در خانه داره + محدوده کالری، AI یک دستور پیشنهاد بده
- B5. **ترک کامل کالری/ماکرو**: کربوهیدرات، چربی، پروتئین + امکان افزودن آیتم جدید به برنامه
- B6. **ثبت با صدا**: کاربر با ویس بگه چی خورده، AI خودش کالری/ماکرو رو تشخیص و ثبت کنه

### دسته C — تمرین هوشمند
- C1. **پنل اختصاصی تمرین** (تفکیک UI به ۳ حوزه: تغذیه / تمرین / استمرار، با دسترسی به AI coach و human coach در هر بخش)
- C2. **ثبت هوشمند جلسه تمرین**: کاربر فقط تیک می‌زنه کدوم ست‌ها رو زده؛ شدت فشار، حس بعد تمرین، رضایت از تمرین به‌عنوان معیار برای تحلیل AI
- C3. **PR Tracking**: تشخیص رکورد جدید (وزنه/تکرار) نسبت به قبل
- C4. **ثبت با صدا**: کاربر حرکات و PR روزش رو با صدا بگه و AI فرم رو پر کنه
- C5. **سازنده برنامه تمرینی هوشمند** بر اساس هدف/سطح/تجهیزات
- C6. **داشبورد دستاوردهای تمرینی**: PRها، امتیاز AI از تمرین، ست‌های هفته، رشد وزنه و…

### دسته D — تشخیص پیشرفت فیزیکی
- D1. آپلود عکس دوره‌ای + تحلیل AI و/یا بررسی مربی
- D2. آموزش پوزینگ اصولی + بانک پوز
- D3. (اختیاری/پیشرفته) تولید تصویر AI از بدن پیش‌بینی‌شده بعد از ۶ ماه/۱ سال طبق روند فعلی

### دسته E — انگیزه و استمرار
- E1. یادآوری میزان رشد به کاربر
- E2. پیگیری با SMS و نوتیف اپ
- E3. نوتیف‌های انگیزشی خودکار
- E4. بخش «Optimal»: کوت‌های انگیزشی

### دسته F — Gamification
- F1. پلتفرم اجتماعی (چت عمومی/گروهی: اشتراک تمرین، عکس بدن، پیشرفت)
- F2. رویدادها: حضوری، مجازی، مسابقه با جایزه/جایزه نقدی (شرکت اختیاری)
- F3. پروفایل کاربر با مدال/مقام/جایزه (مثل: مدال ۵ سال سابقه، مدال تغییر بزرگ، مقام بدن نچرال)
- F4. سیستم امتیاز/مدال چندمنبعی (پیوستگی ثبت غذا، تمرین منظم، PR جدید و…)

### دسته G — مربی انسانی
- G1. جلسات دوره‌ای (آنلاین/حضوری) با تعداد متغیر بر اساس پلن
- G2. پیگیری روزانه از طریق چت
- G3. بررسی دوره‌ای پنل شاگرد توسط مربی (هر ۲-۳ روز) + ارسال فیدبک متنی در برنامه

### دسته H — Deep Dive تحلیل هوش مصنوعی
- H1. تحلیل خودکار هفتگی: امتیاز کلی، بهترین/بدترین روز تمرین، سنگین‌ترین وزنه، طولانی‌ترین/کوتاه‌ترین تمرین، رشد نسبت به هفته قبل در حرکت خاص
- H2. تحلیل ماهانه (تجمیع هفته‌ها)
- H3. تغذیه این تحلیل به بخش Gamification/امتیاز

### دسته I — تضمین‌ها (عمدتاً منطق کسب‌وکار / CRM، کم‌تکنیکال)
- I1. تضمین مشروط نتیجه (اگر همه‌چیز رعایت شد ولی نتیجه نیومد → ادامه رایگان)
- I2. تضمین کیفیت خدمات (اگر کیفیت مطلوب نبود → بازگشت وجه یا ادامه رایگان)

---

## ۲) روش‌های پیاده‌سازی (خلاصه تحقیق فنی)

| فیچر | روش پیشنهادی | چرا مناسب استک فعلی است |
|------|----------------|--------------------------|
| **محاسبه BMR/TDEE و ماکرو** | فرمول Mifflin-St Jeor (یا Katch-McArdle اگر % چربی بدن موجود باشه) روی سرور Go، بدون نیاز به AI برای خود محاسبه | داده‌های لازم (`HeightCm`, `WeightKg`, `BirthDate`, `Gender`, `BodyFatPercent`) از قبل روی مدل `User` هست |
| **تولید متن برنامه غذایی/تمرینی توسط AI** | استفاده از **Structured Outputs / function calling** OpenAI (نه فقط چت آزاد) — AI را مجبور کن یک JSON schema مشخص (روزها → وعده‌ها → آیتم‌ها با کالری/ماکرو) برگردونه، بعد سرور آن را اعتبارسنجی و ذخیره کنه | همین امروز هم `OPENAI_API_KEY` و `ai_chat_controller` وجود دارد؛ فقط باید از حالت «چت آزاد» به «schema-constrained» تغییر کنه تا خروجی قابل ذخیره در `nutrition_program`/`workout_program` باشه، نه صرفاً متن نمایشی |
| **ثبت با صدا (ویس‌به‌داده)** | ۱) STT (مثلاً Whisper API) برای تبدیل صدا به متن فارسی → ۲) همان متن به تابع function-calling در بند بالا داده بشه تا مستقیماً به `daily_food_log` / `workout_set_log` تبدیل بشه | یک pipeline دو مرحله‌ای ساده روی همان کنترلر AI؛ فایل صوتی مثل آپلود عکس فعلی (`uploads/`) ذخیره و به OpenAI پاس داده می‌شه |
| **بانک غذا/دستور پخت با فیلتر** | مدل جدید `recipe.go` (شبیه `food.go` موجود) با فیلدهای کالری، نوع رژیم، ویدیو (لینک یا آپلود)، تگ‌ها؛ endpoint فیلتر مثل الگوی `admin_exercise`/`coach_food` موجود | همان الگوی CRUD + کاتالوگ که برای `exercise.go`/`food.go` از قبل پیاده‌سازی شده، کپی می‌شه |
| **تشخیص پیشرفت از عکس** | ذخیره عکس در `uploads/` (مثل الان) + فراخوانی OpenAI Vision با یک پرامپت ساختاریافته که فقط توصیف کیفی برمی‌گردونه (نه تشخیص پزشکی)؛ مربی می‌تونه override کنه | نیازی به مدل بینایی جدا نیست، از همون OpenAI key استفاده می‌شه؛ نکته: خروجی باید صرفاً «مشاهده‌محور» باشه نه ادعای پزشکی، برای جلوگیری از مسئولیت حقوقی |
| **تصویر AI «بدن آینده»** | فیچر پرریسک/اختیاری — از نظر فنی با APIهای تولید تصویر (image edit/inpainting) شدنیه، اما باید به‌صورت واضح disclaimer «شبیه‌سازی، نه تضمین پزشکی» داشته باشه. پیشنهاد: در فازهای اولیه پیاده نشه، فاز آخر و صرفاً optional | تعهدات حقوقی نتیجه‌گرا (تضمین‌های بخش I) با ادعای بصری «بدن آینده» تداخل داره؛ باید با احتیاط طراحی بشه |
| **PR Tracking و امتیاز هفتگی/ماهانه** | Job زمان‌بندی‌شده (cron در Go، مثلاً با `robfig/cron`) که هر شنبه/آخر ماه روی `workout_set_log` + `daily_food_log` کوئری می‌زنه، دلتا نسبت به دوره قبل حساب می‌کنه، و در صورت نیاز خلاصه را به AI می‌ده تا متن تحلیلی (نه اعداد) تولید کنه | جدا کردن «محاسبه عددی» (قطعی، در Go) از «تولید متن انگیزشی» (AI) دقت را تضمین می‌کنه و هزینه توکن AI رو کم می‌کنه |
| **نوتیفیکیشن/یادآوری** | **Firebase Cloud Messaging (FCM)** برای موبایل (سرویس push استاندارد Flutter) + مدل موجود `notification.go` برای درون‌اپ + سرویس SMS موجود (`sms_service.go`) برای پیگیری خارج از اپ | هم موبایل هم بک‌اند از قبل زیرساخت دارن (`sms_service.go`، `notification.go`)؛ فقط باید FCM token روی `mobile_device.go` ذخیره و یک `notification_service` برای ارسال زمان‌بندی‌شده اضافه بشه |
| **گیمیفیکیشن / مدال‌ها** | مدل جدید `achievement_rule.go` (تعریف قانون) + `user_achievement.go` (رکورد اعطاشده) — event-driven: هر عملیات مهم (ثبت غذا، PR جدید، تکمیل هفته) یک event می‌فرسته و سرویس امتیازدهی چک می‌کنه قانون‌ها رو | الگوی مشابه `coach_achievement.go` موجود (که فعلاً فقط برای مربی‌هاست) برای دانشجو هم تکرار می‌شه |
| **پلتفرم اجتماعی/چت گروهی** | برای فاز اول: **Read-model ساده** (پست‌های متنی/عکس + کامنت + لایک، بدون real-time)؛ اگر چت زنده لازم شد، در فاز بعدی WebSocket (gorilla/websocket با Gin سازگاره) اضافه بشه | شروع با HTTP polling ساده هزینه و ریسک کمتری داره تا از ابتدا real-time بسازیم؛ می‌شه بعداً ارتقا داد |

---

## ۳) فازبندی اجرایی (Backlog قابل اجرا)

> هر فاز مستقل قابل تحویله؛ ترتیب طوری چیده شده که فازهای بعدی روی داده/زیرساخت فازهای قبلی سوار بشن.
> هر تسک شامل لایه (Backend / Frontend / Mobile) هست تا مستقیم قابل تبدیل به Issue باشه.

### 🟦 فاز ۰ — زیرساخت مشترک AI (پیش‌نیاز همه فازها)
هدف: تبدیل `ai_chat_controller` از «چت آزاد» به موتوری که می‌تونه خروجی ساختاریافته و قابل‌ذخیره تولید کنه.

- [ ] **BE-0.1** طراحی JSON schemaهای پایه: `NutritionPlanSchema`, `WorkoutPlanSchema`, `FoodLogSchema`, `SetLogSchema`
- [ ] **BE-0.2** اضافه‌کردن `ai_service.GenerateStructured(schema, context)` با function calling / structured outputs OpenAI
- [ ] **BE-0.3** لایه اعتبارسنجی خروجی AI قبل از ذخیره در DB (جلوگیری از داده مخرب/نامعتبر)
- [ ] **BE-0.4** لاگ کامل درخواست/پاسخ AI برای دیباگ و رعایت هزینه توکن (جدول `ai_request_log`)
- [ ] **BE-0.5** مدل جدید `ai_persona` یا enum برای تفکیک «مربی AI تغذیه» / «مربی AI تمرین» / «مربی AI عمومی» در همان `ai_chat_controller`

### 🟩 فاز ۱ — تغذیه هوشمند (پایه)
- [ ] **BE-1.1** فیلد محاسبه‌شده BMR/TDEE در `service/nutrition_calc.go` (فرمول Mifflin-St Jeor از فیلدهای موجود User)
- [ ] **BE-1.2** endpoint `POST /me/nutrition/generate` → از schema فاز ۰ استفاده کن، خروجی رو در `nutrition_program` ذخیره کن
- [ ] **BE-1.3** مدل `nutrition_goal` (کات/بالک/نگهداری) اضافه به `nutrition_program`
- [ ] **FE-1.4** صفحه «ساخت برنامه تغذیه با AI» در `/user/food-diary` یا صفحه جدید `/user/nutrition/generate`
- [ ] **Mobile-1.5** پاریتی صفحه بالا در `features/food_diary`
- [ ] **BE-1.6** مدل جدید `recipe.go` (بانک دستور پخت): کالری، نوع رژیم، مواد، ویدیو، توضیحات
- [ ] **BE-1.7** endpoint فیلتر `GET /recipes?calorie_min=&calorie_max=&type=&ingredients=`
- [ ] **FE/Mobile-1.8** صفحه «بانک غذا» با فیلتر (مشابه الگوی `admin_exercises`)
- [ ] **BE-1.9** endpoint `POST /me/nutrition/suggest-from-ingredients` (دستور بداهه از مواد موجود)

### 🟩 فاز ۲ — ترک کالری/ماکرو کامل + ثبت با صدا
- [ ] **BE-2.1** گسترش `daily_food_log` با فیلدهای carbs/protein/fat اگر ناقصه
- [ ] **FE/Mobile-2.2** UI افزودن دستی آیتم با ماکروی کامل
- [ ] **BE-2.3** endpoint `POST /me/food-logs/voice` (آپلود صوت)
- [ ] **BE-2.4** pipeline: STT → متن → function calling با schema `FoodLogSchema` → ذخیره در `daily_food_log`
- [ ] **FE/Mobile-2.5** دکمه میکروفون در صفحه food-diary + پیش‌نمایش قبل از تایید ثبت (کاربر تایید نهایی بده، AI فقط پیشنهاد بده)

### 🟨 فاز ۳ — تمرین هوشمند (ثبت + PR + برنامه)
- [ ] **BE-3.1** فیلدهای effort/RPE و satisfaction روی `workout_set_log` یا `workout_session` (اگر موجود نیست)
- [ ] **BE-3.2** سرویس تشخیص PR: هنگام ثبت ست جدید، مقایسه با رکورد قبلی کاربر برای همان حرکت
- [ ] **BE-3.3** endpoint `POST /me/workout/generate` با schema `WorkoutPlanSchema` → ذخیره در `workout_program`
- [ ] **FE-3.4** صفحه «ساخت برنامه تمرینی با AI»
- [ ] **BE-3.5** endpoint `POST /me/workout/sessions/voice` (همون الگوی pipeline فاز ۲ برای تمرین)
- [ ] **FE/Mobile-3.6** UI ثبت جلسه: چک‌لیست ست‌ها + اسلایدر شدت فشار + ایموجی حس بعد تمرین
- [ ] **Mobile-3.7** پاریتی کامل صفحات بالا

### 🟨 فاز ۴ — داشبورد دستاورد و تحلیل Deep Dive
- [ ] **BE-4.1** جاب Cron هفتگی (`robfig/cron` یا مشابه): آمارگیری از `workout_set_log` + `daily_food_log` هفته گذشته
- [ ] **BE-4.2** جدول `weekly_report` / `monthly_report` (اعداد خام: بهترین روز، سنگین‌ترین وزنه، رشد و…)
- [ ] **BE-4.3** فراخوانی AI برای تبدیل اعداد فاز ۴.۲ به متن تحلیلی انسانی (خلاصه هفتگی)
- [ ] **FE/Mobile-4.4** صفحه «داشبورد پیشرفت» با نمودار (Recharts موجود در فرانت)
- [ ] **BE-4.5** endpoint نمایش تاریخچه گزارش‌های هفتگی/ماهانه

### 🟧 فاز ۵ — عکس پیشرفت و پوزینگ
- [ ] **BE-5.1** مدل `progress_photo.go` (تاریخ، URL، تگ پوز، وضعیت بررسی مربی)
- [ ] **BE-5.2** endpoint آپلود + فراخوانی AI Vision با پرامپت مشاهده‌محور (نه تشخیص پزشکی)
- [ ] **BE-5.3** مدل `pose_bank.go` (نام پوز، ویدیو آموزشی، توضیح)
- [ ] **FE/Mobile-5.4** گالری قبل/بعد + صفحه بانک پوز
- [ ] **Coach-5.5** پنل مربی برای بررسی/تایید یا رد تحلیل AI روی عکس
- [ ] *(اختیاری/فاز جدا)* **BE-5.6** تولید تصویر «بدن آینده» با disclaimer الزامی — پیشنهاد: بعد از تثبیت فازهای اصلی

### 🟪 فاز ۶ — Gamification (امتیاز و مدال)
- [ ] **BE-6.1** مدل `achievement_rule.go` (نوع رویداد، شرط، امتیاز/مدال)
- [ ] **BE-6.2** مدل `user_achievement.go` (کاربر، مدال، تاریخ اعطا)
- [ ] **BE-6.3** سرویس event-driven: هوک روی ثبت غذا/تمرین/PR/تداوم برای چک قوانین
- [ ] **BE-6.4** endpoint `GET /me/achievements`
- [ ] **FE/Mobile-6.5** بخش مدال‌ها در `/user/profile`
- [ ] **BE-6.6** قوانین اولیه: مدال «۵ سال سابقه» (از تاریخ عضویت/سابقه اعلامی)، «تداوم ۳۰ روزه ثبت غذا»، «PR جدید»، «تغییر بزرگ سالانه» (نیازمند بررسی داده تصویر/وزن)

### 🟪 فاز ۷ — پلتفرم اجتماعی و رویدادها
- [ ] **BE-7.1** مدل `community_post.go` (متن، عکس، نویسنده) + `post_comment.go` + `post_like.go`
- [ ] **BE-7.2** endpoint فید عمومی با pagination
- [ ] **BE-7.3** مدل `event.go` (نوع: حضوری/مجازی، جایزه، تاریخ) + `event_participation.go` (شرکت اختیاری)
- [ ] **FE/Mobile-7.4** صفحه فید اجتماعی + صفحه رویدادها
- [ ] **Admin-7.5** پنل مدیریت رویدادها/مسابقات در `/admin`
- [ ] *(فاز بعدی، اختیاری)* **BE-7.6** ارتقا فید به WebSocket برای چت زنده

### 🟥 فاز ۸ — انگیزه و استمرار (نوتیفیکیشن)
- [ ] **BE-8.1** یکپارچه‌سازی FCM: سرویس `push_notification_service.go`
- [ ] **BE-8.2** ذخیره FCM token روی `mobile_device.go` موجود
- [ ] **BE-8.3** جاب زمان‌بندی‌شده یادآوری (بی‌فعالیتی ۲۴/۴۸ ساعته، یادآوری هفتگی رشد)
- [ ] **BE-8.4** اتصال به `sms_service.go` موجود برای پیگیری‌های مهم (مثلاً عدم ثبت طولانی‌مدت)
- [ ] **BE-8.5** مدل `motivational_quote.go` + endpoint تصادفی برای بخش «Optimal»
- [ ] **FE/Mobile-8.6** بخش «Optimal» در داشبورد + تنظیمات کاربر برای فعال/غیرفعال کردن نوتیف

### 🟥 فاز ۹ — گردش‌کار مربی انسانی
- [ ] **BE-9.1** مدل `coach_session.go` (تاریخ، نوع حضوری/آنلاین، وضعیت، یادداشت)
- [ ] **BE-9.2** endpoint زمان‌بندی جلسه توسط مربی/دانشجو
- [ ] **BE-9.3** سیستم یادآوری بررسی دوره‌ای شاگرد برای مربی (هر ۲-۳ روز) — چک‌لیست در `/coach/students/[id]`
- [ ] **BE-9.4** endpoint ارسال فیدبک متنی مربی به شاگرد (متصل به `notification.go`)
- [ ] **FE-9.5** تقویم جلسات در پنل مربی و دانشجو

### ⬛ فاز ۱۰ — تضمین‌ها (عمدتاً کسب‌وکاری)
- [ ] **BE-10.1** مدل `guarantee_case.go` (کاربر، نوع تضمین، وضعیت شرایط، تاریخ شروع/پایان پلن)
- [ ] **BE-10.2** چک‌لیست شرایط تضمین مشروط (٪ تکمیل تغذیه، ٪ تکمیل تمرین، تعداد جلسات مربی) — محاسبه خودکار از داده فازهای قبلی
- [ ] **Admin-10.3** پنل بررسی و تایید/رد درخواست تضمین در `/admin`
- [ ] **FE-10.4** صفحه «وضعیت تضمین من» در `/user`

---

## ۴) ترتیب پیشنهادی اجرا (اولویت‌بندی)

```
فاز ۰ (زیرساخت AI)
   │
   ├─► فاز ۱ (تغذیه پایه) ──► فاز ۲ (ماکرو + صدا)
   │
   ├─► فاز ۳ (تمرین هوشمند) ──► فاز ۴ (Deep Dive)
   │
   ├─► فاز ۵ (عکس/پوزینگ) [می‌تونه موازی با ۳-۴ باشه]
   │
   └─► فاز ۶ (Gamification) — نیازمند داده فازهای ۱-۵
          │
          ├─► فاز ۷ (اجتماعی/رویداد)
          └─► فاز ۸ (نوتیف/انگیزه)

فاز ۹ (مربی انسانی) — مستقل، هر زمان قابل شروع
فاز ۱۰ (تضمین‌ها) — آخر از همه، چون به داده تکمیل‌شده همه فازها برای محاسبه شرایط نیاز داره
```

**توصیه:** فاز ۰ و فاز ۱ رو اول تمام کن — چون هم زیرساخت AI ساختاریافته پایه همه فیچرهای بعدیه، هم سریع‌ترین ارزش قابل‌لمس برای کاربر رو می‌ده (برنامه تغذیه هوشمند).

---

*نکته حقوقی/محصولی: در فیچرهای D3 (تصویر بدن آینده) و I (تضمین‌ها)، حتماً disclaimer روشن اضافه کن که خروجی‌های AI جنبه شبیه‌سازی/راهنما دارن و جایگزین نظر پزشک/متخصص تغذیه نیستن — هم برای کاهش ریسک حقوقی هم برای شفافیت با کاربر.*
